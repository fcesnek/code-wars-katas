/**
 * https://www.codewars.com/kata/caesar-cipher-encryption-variation/train/javascript
 * Your task is to write a function that will encrypt a phrase using Caesar Cipher and a given
 * shift key and return it as a string. However, the key should be increased by 1 with every word in a phrase.
 * @param  {String} phrase
 * @param  {Number} shift
 * @return {String}
 */
function caesarEncode(phrase, shift) {
  const encoded = '';

  console.log(phrase.split(' '));

  return encoded;
}

console.log(caesarEncode('alea iacta est', 3) == 'dohd megxe jxy');
// console.log(caesarEncode('conquer et impera', 13) == 'pbadhre sh xbetgp');
